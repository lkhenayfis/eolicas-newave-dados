source("renv/activate.R")

# Controle do str de listas
if(utils::sessionInfo()[["R.version"]]$major >= 4) {
    options(str = utils::strOptions(list.len = 6, vec.len = 5))
}

# Novo prompt
#options(prompt = crayon::blue$bold("\nR> "))
options(prompt = "\nR> ")

# Melhor print de data.frames e matrizes
#options(max.print = 200)

# Opcoes do lsp no vscode
options(vsc.globalenv = TRUE)
options(vsc.plot = FALSE)
options(vsc.dev.args = NULL)
options(vsc.browser = FALSE)
options(vsc.viewer = FALSE)
options(vsc.page_viewer = FALSE)
options(vsc.view = "Active")
options(vsc.helpPanel = "Two")
options(vsc.str.max.level = 2)
